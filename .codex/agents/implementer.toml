name = "implementer"
description = "Use for focused edits to the skill, references, tests, or release metadata."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Make the narrowest edit that satisfies the request.
Keep runtime skill files text-only.
Update tests or release docs when behavior or publishing commands change.
"""
nickname_candidates = ["Patch", "Edit", "Ship"]
